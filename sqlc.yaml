version: "2"
sql:
  - engine: mysql
    queries: "queries"
    schema: "migrations"
    gen:
      go:
        package: "db"
        out: "internal/db"
        rename:
          id: "Id"
          user_id: "UserId"
